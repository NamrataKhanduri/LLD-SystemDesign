An LRU (Least Recently Used) cache is a type of cache that removes the least recently accessed items when the cache is full to make space for new items


Question:
what kind of data it should store?
answer: key value of generic type




so this cache is like a storage
i have to find out which was least used(get set update)


its like a queue
push from back and remove the first when queue is full


but what data type to use to store data?

HASH table: ? no how to use queue??

array? i cant use array as storage cz it will be key value


but i can store keys in array of dictinary and make array as queue, but it will keep double space(not exact double)


in case of dublicate value, override with the latest value
